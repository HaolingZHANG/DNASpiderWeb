<p align="center">
<img src="../logo.svg" alt="DNA Spider-Web" title="DNASpiderWeb" width="40%"/>
</p>

---

You can generate the results (12.7GB) by yourself (as step 1 in pipeline folder), it only takes several hours.

Ignored file list:
```html
├── BM1[v].npy    // Valid vertices directly obtained by biochemical filter 1
├── BM2[v].npy    // Valid vertices directly obtained by biochemical filter 2
├── BM3[v].npy    // Valid vertices directly obtained by biochemical filter 3
├── BM4[v].npy    // Valid vertices directly obtained by biochemical filter 4
├── BM5[v].npy    // Valid vertices directly obtained by biochemical filter 5
├── BM6[v].npy    // Valid vertices directly obtained by biochemical filter 6
├── BM7[v].npy    // Valid vertices directly obtained by biochemical filter 7
├── BM8[v].npy    // Valid vertices directly obtained by biochemical filter 8

├── BM1[g].npy    // Directly generated graph from valid vertices with biochemical filter 1
├── BM2[g].npy    // Directly generated graph from valid vertices with biochemical filter 2
├── BM3[g].npy    // Directly generated graph from valid vertices with biochemical filter 3
├── BM4[g].npy    // Directly generated graph from valid vertices with biochemical filter 4
├── BM5[g].npy    // Directly generated graph from valid vertices with biochemical filter 5
├── BM6[g].npy    // Directly generated graph from valid vertices with biochemical filter 6
├── BM7[g].npy    // Directly generated graph from valid vertices with biochemical filter 7
├── BM8[g].npy    // Directly generated graph from valid vertices with biochemical filter 8

├── FLC1[v].npy   // Saved fixed-length vertices with biochemical filter 1
├── FLC2[v].npy   // Saved fixed-length vertices with biochemical filter 2
├── FLC3[v].npy   // Saved fixed-length vertices with biochemical filter 3
├── FLC4[v].npy   // Saved fixed-length vertices with biochemical filter 4
├── FLC5[v].npy   // Saved fixed-length vertices with biochemical filter 5
├── FLC6[v].npy   // Saved fixed-length vertices with biochemical filter 6
├── FLC7[v].npy   // Saved fixed-length vertices with biochemical filter 7
├── FLC8[v].npy   // Saved fixed-length vertices with biochemical filter 8

├── FLC1[g].npy   // Fixed-length graph coder with biochemical filter 1
├── FLC2[g].npy   // Fixed-length graph coder with biochemical filter 2
├── FLC3[g].npy   // Fixed-length graph coder with biochemical filter 3
├── FLC4[g].npy   // Fixed-length graph coder with biochemical filter 4
├── FLC5[g].npy   // Fixed-length graph coder with biochemical filter 5
├── FLC6[g].npy   // Fixed-length graph coder with biochemical filter 6
├── FLC7[g].npy   // Fixed-length graph coder with biochemical filter 7
├── FLC8[g].npy   // Fixed-length graph coder with biochemical filter 8

├── VLC1[v].npy   // Saved variable-length vertices with biochemical filter 1
├── VLC2[v].npy   // Saved variable-length vertices with biochemical filter 2
├── VLC3[v].npy   // Saved variable-length vertices with biochemical filter 3
├── VLC4[v].npy   // Saved variable-length vertices with biochemical filter 4
├── VLC5[v].npy   // Saved variable-length vertices with biochemical filter 5
├── VLC6[v].npy   // Saved variable-length vertices with biochemical filter 6
├── VLC7[v].npy   // Saved variable-length vertices with biochemical filter 7
├── VLC8[v].npy   // Saved variable-length vertices with biochemical filter 8

├── VLC1[g].npy   // Variable-length graph coder with biochemical filter 1
├── VLC2[g].npy   // Variable-length graph coder with biochemical filter 2
├── VLC3[g].npy   // Variable-length graph coder with biochemical filter 3
├── VLC4[g].npy   // Variable-length graph coder with biochemical filter 4
├── VLC5[g].npy   // Variable-length graph coder with biochemical filter 5
├── VLC6[g].npy   // Variable-length graph coder with biochemical filter 6
├── VLC7[g].npy   // Variable-length graph coder with biochemical filter 7
├── VLC8[g].npy   // Variable-length graph coder with biochemical filter 8
```


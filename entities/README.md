<p align="center">
<img src="../logo.svg" alt="DNA Spider-Web" title="DNASpiderWeb" width="40%"/>
</p>

---

You can generate the results (360MB) by yourself (as step 1 in pipeline folder), it only takes several minutes.

Ignored file list:
```html
├── BM1[vertices].npy  // Valid vertices directly obtained by biochemical filter 1
├── BM2[vertices].npy  // Valid vertices directly obtained by biochemical filter 2
├── BM3[vertices].npy  // Valid vertices directly obtained by biochemical filter 3
├── BM4[vertices].npy  // Valid vertices directly obtained by biochemical filter 4
├── BM5[vertices].npy  // Valid vertices directly obtained by biochemical filter 5
├── BM6[vertices].npy  // Valid vertices directly obtained by biochemical filter 6

├── BM1[graph].npy     // Directly generated graph from valid vertices with biochemical filter 1
├── BM2[graph].npy     // Directly generated graph from valid vertices with biochemical filter 2
├── BM3[graph].npy     // Directly generated graph from valid vertices with biochemical filter 3
├── BM4[graph].npy     // Directly generated graph from valid vertices with biochemical filter 4
├── BM5[graph].npy     // Directly generated graph from valid vertices with biochemical filter 5
├── BM6[graph].npy     // Directly generated graph from valid vertices with biochemical filter 6

├── FLC1[vertices].npy      // Saved fixed-length vertices with biochemical filter 1
├── FLC2[vertices].npy      // Saved fixed-length vertices with biochemical filter 2
├── FLC3[vertices].npy      // Saved fixed-length vertices with biochemical filter 3
├── FLC4[vertices].npy      // Saved fixed-length vertices with biochemical filter 4
├── FLC5[vertices].npy      // Saved fixed-length vertices with biochemical filter 5
├── FLC6[vertices].npy      // Saved fixed-length vertices with biochemical filter 6

├── FLC1[graph].npy         // Fixed-length graph coder with biochemical filter 1
├── FLC2[graph].npy         // Fixed-length graph coder with biochemical filter 2
├── FLC3[graph].npy         // Fixed-length graph coder with biochemical filter 3
├── FLC4[graph].npy         // Fixed-length graph coder with biochemical filter 4
├── FLC5[graph].npy         // Fixed-length graph coder with biochemical filter 5
├── FLC6[graph].npy         // Fixed-length graph coder with biochemical filter 6

├── VLC1[vertices].npy      // Saved variable-length vertices with biochemical filter 1
├── VLC2[vertices].npy      // Saved variable-length vertices with biochemical filter 2
├── VLC3[vertices].npy      // Saved variable-length vertices with biochemical filter 3
├── VLC4[vertices].npy      // Saved variable-length vertices with biochemical filter 4
├── VLC5[vertices].npy      // Saved variable-length vertices with biochemical filter 5
├── VLC6[vertices].npy      // Saved variable-length vertices with biochemical filter 6

├── VLC1[graph].npy         // Variable-length graph coder with biochemical filter 1
├── VLC2[graph].npy         // Variable-length graph coder with biochemical filter 2
├── VLC3[graph].npy         // Variable-length graph coder with biochemical filter 3
├── VLC4[graph].npy         // Variable-length graph coder with biochemical filter 4
├── VLC5[graph].npy         // Variable-length graph coder with biochemical filter 5
├── VLC6[graph].npy         // Variable-length graph coder with biochemical filter 6
```

